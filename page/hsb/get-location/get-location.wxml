<!--<import src="../common/header.wxml" />-->
<!--<import src="../common/footer.wxml" />-->

<view style="display:flex">
    <icon type="search" size="24" />
    <input  bindinput="bindKeyInput" value="{{searchValue}}" placeholder="搜索" style="font-size:16px" />
    <button size="mini" bindtap="closeSearch">取消</button>
</view>

<view hidden="{{!!searchValue}}">
    <!--<scroll-view scroll-y="true">-->
        <view wx:for-items="{{lbs.items}}" data-name="{{item.name}}" bindtap="bindTap">
            <text>{{item.name}}</text>
            <text style="font-size:12px;color:#999">{{lbs.detail.city}}-{{lbs.detail.district}}</text>
        </view>
    <!--</scroll-view>-->
</view>


<view hidden="{{!searchValue}}">
    <!--<scroll-view scroll-y="true">-->
        <view
            wx:for-items="{{search}}"
            data-city="{{item.city}}"
            data-cityCode="{{item.cityCode}}"
            data-district="{{item.district}}"
            data-name="{{item.name}}"
            data-xxxx="{{item.xxxx}}"
            data-yyyy="{{item.yyyy}}"
            bindtap="bindTap2"
        >
          <text>{{item.name}}</text>
          <text style="font-size:12px;color:#999">{{item.city}}-{{item.district}}</text>
        </view>
    <!--</scroll-view>-->
</view>

<view class="container">
  <template is="header" data="{{title: 'getLocation'}}"/>

  <view class="page-body">
    <view class="page-body-wrapper">
      <view class="page-body-info">
        <text class="page-body-text-small">当前位置经纬度</text>
        <block wx:if="{{hasLocation === false}}">
          <text class="page-body-text">未获取</text>
        </block>
        <block wx:if="{{hasLocation === true}}">
          <view class="page-body-text-location">
            <text>E{{location.longitude}}′</text>
            <text>N{{location.latitude}}′</text>
          </view>
        </block>
      </view>
      <view class="page-body-buttons">
        <!--<button class="page-body-button" type="primary" bindtap="getLocation">获取位置</button>-->
        <!--<button class="page-body-button" bindtap="clear">清空</button>-->
      </view>
    </view>
  </view>

  <template is="footer" />
</view>


