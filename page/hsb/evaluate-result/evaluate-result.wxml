<view class="container">

    <view class="top-bar" >
        若最终检测结果与您评估情况一致，您将在24小时内以当前价格收款
    </view>
    
    <scroll-view scroll-y="true" class="scroll-bg" >
        <view class="evaluate-price-bg">
            <text class="evaluate-title-text">估价结果</text>
            <view class="evaluate-price-text-bg" >
                <view class="evaluate-price-text" >
                    <view class="rmb-black-text">¥</view>{{evaluatePrice}}
                </view>
            </view>
            <view class="reevaluate-view">
                <view class="reevaluate-text" bindtap="reevaluate">重新估价</view>
                <view class="reevaluate-sj"></view>
            </view>
        </view>

        <view style="width : 100%; height:1rpx; background-color : #f5f5f5;margin-bottom:20rpx;" />
        <view class="answer-items-bg" >
            <view style="display : flex; flex-wrap: wrap;width:100%;justify-content: flex-start;" >
                <view class="answer-item" wx:for="{{answerArray}}" wx:key="*this">{{item}}</view>
            </view>
            <image src="/image/icon/{{isExpanded ? 16 : 7}}.png" style="width: 32rpx;height: 32rpx;" mode="aspectFit" bindtap="onExpandBtnClick" ></image>
        </view>

        <view class="falldown-text-bg" style="font-size: 26rpx;color: #999999;">
            预计下月跌<image src="/image/icon/5.png" style="width: 22rpx;height: 22rpx;" mode="aspectFit" ></image><text style="color: #FD2626;" >{{downPrice}}</text>元，与其闲置,不如换钱买买买
        </view>
        
        <view class="chart-title-bg" >
            <view class="hsb-quotation" >
                <view class="hsb-quotation-line" />
                <text style="font-size:26rpx;color:#F9BE04" >回收宝报价</text>
            </view>
            <view class="market-quotation" >
                <view class="market-quotation-line" />
                <text style="font-size:26rpx;color:#4FC7FE" >市场均价</text>
            </view>
        </view>
       
        <view class="chart-view">
            <view class="chart-left">
                <text class="y-text" wx:for="{{values}}" >{{item}}</text>
            </view>
            <view class="chart-right">
                <image src="/image/icon/chart.png" class="chart-image"/>
                <view class="chart-x">
                    <text class="x-text" wx:for="{{month}}" wx:key="*this" >{{item}}月</text>
                </view>
            </view>
        </view>

        <view style="display:flex;flex: 1;" />
        <text style="font-size:26rpx;color:#888888;margin:14rpx;display:flex;justify-content: center;align-items: center;" >手机回收成功后，我们会付款到你的微信账户</text>
        <view class="bottom-btn-enable" bindtap="onRecycleBtnClick" >立即回收</view>
        
        <view class="evaluate-result">
            <view class="result-item" bindtap="switchInfo" wx:for="{{hsbFeature}}" data-index="{{index}}">
                <image src="{{item.img}}" class="item-image"/>
                <text class="item-text">{{item.text}}</text>
            </view>
        </view>
        <view class="tip-sj marginL0" wx:if="{{index==0}}"></view>
        <view class="tip-sj marginL1" wx:if="{{index==1}}"></view>
        <view class="tip-sj marginL2" wx:if="{{index==2}}"></view>
        <view class="tip-sj marginL3" wx:if="{{index==3}}"></view>
        <view class="tip-view">{{tipInfo}}</view>
    </scroll-view>
</view>